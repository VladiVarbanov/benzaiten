# Managed-work V0 implementation log

This ledger records only architecture-relevant progress since the latest deep review. It is not a changelog and does not replace Git history or inspection of the source being changed.

## Starting an implementation iteration

1. Read the [latest architecture review](review/2026-09-08_codex_review.md).
2. Read the implementation entries below that postdate that review.
3. Inspect the relevant Archify target/current maps in `workspace/`.
4. Inspect the actual source files involved in the requested change.
5. Treat actual source as final implementation truth wherever documentation is stale.
6. Rescan the wider repository only when the requested change genuinely requires it.

## Review cadence

Normally perform a new deep architecture review after about 5–6 meaningful managed-work implementation iterations. Review earlier when architecture materially changes, a protocol changes, a frozen assumption is overturned, implementation starts diverging from the latest Archify target, or a significant new subsystem is introduced. Compare a new review with the preceding review instead of rediscovering the project without context.

## Archify feedback loop

Before implementation, the relevant target map shows the coding agent's understanding of the requested architecture. After an architecture-affecting implementation, update the relevant current map to show what the source now implements. The comparison should expose semantic decisions leaking into Task Executive, Plan being treated as an active decision maker, extra manager/coordinator layers, incorrect model-role relationships, and bypassed protocol boundaries.

Archify is development documentation/tooling, not a Benzaiten runtime dependency. Do not regenerate maps for purely mechanical changes; update only a map whose represented architecture changed.

Current map set:

- `workspace/benzaiten_current.architecture.html` — current implemented surface
- `workspace/benzaiten_v0_target.architecture.html` — target V0 component architecture
- `workspace/benzaiten_normal_planning.architecture.html` — target Normal planning roles and flow
- `workspace/benzaiten_managed_work_runtime.html` — target managed-work runtime boundary
- `workspace/benzaiten_managed_work_v0_target.architecture.html` — frozen pre-implementation V0 target

## Baseline

- Latest deep review: 2026-09-08 Codex review, based on commit `959bdff7b83fec64ee46309db180e691f4501fb3`.
- Frozen pre-implementation target: `workspace/benzaiten_managed_work_v0_target.architecture.html`.
- Managed-work implementation entries since that review: none yet.
- Known post-review target clarification: `workspace/benzaiten_v0_architecture_delta.md` and the Normal-planning map specify reciprocal constructive assessments; the saved review's first-slice sequence specifies only Qwen's assessment of Gemma. This is a target-document change, not an implemented source change.
- Known stale documentation: the existing decision schematics contain `JobContext`, contrary to the frozen V0 boundary. They are not implementation authority.

## Entry format

### Iteration N — YYYY-MM-DD

- Goal:
- Files changed:
- Architectural effect:
- Tests/validation:
- Unresolved issue:
- Archify map changed: No, or Yes — `<map path and change>`

### Iteration 1 — 2026-09-11

- Goal: Align the DirectorTask, Plan, TaskExecution, initialization, hardware, and model-role contracts with the frozen Managed-Work V0 target; no planning orchestration or live model calls.
- Files changed: `src/director.py`, `src/orchestrator.py`, `src/task_execution.py`, `src/config.py`, `src/initialization.py`, `protocols/planning/plan_protocol_v0.json`, `protocols/execution/task_execution_vocabulary.yaml`, `workspace/.benzaiten_layout.json`, focused `smoke_tests/`, and the CURRENT Archify JSON/HTML/visual-check receipt.
- Architectural effect: The existing DirectorTask is now a strict 19-field protocol adapter; deterministic validation certifies task and terminal-execution structure; managed preparation treats `DirectorTask.action` as authoritative while the legacy action-state path remains intact. No semantic authority moved into Task Executive.
- Tests/validation: Focused suite 28 passed; full suite 47 passed; `git diff --check` passed; CURRENT Archify showcase validation passed 9/9 with zero errors/warnings and delivery succeeded. Automated browser visual-check was skipped because Chrome/Chromium is unavailable.
- Unresolved issue: No Patch 1 blocker. The reciprocal Normal-planning loop, managed ModelClient execution, TaskExecution builder, and persistence remain intentionally unimplemented for later iterations.
- Archify map changed: Yes — `workspace/benzaiten_current.architecture.json` and `.html` now show the implemented strict DirectorTask/Task Executive seam and terminal-profile validation. The frozen V0 TARGET map did not change.

### Iteration 2 — 2026-09-11

- Goal: Implement the fixed five-call reciprocal Normal-planning sequence through validated final Plan construction only; no DirectorTask generation, execution, TaskExecution, or ACCEPT/REVISE.
- Files changed: `src/director.py`, `src/planning.py`, `src/orchestrator.py`, `src/config.py`, `smoke_tests/test_normal_planning.py`, this ledger, and the CURRENT Archify JSON/HTML/visual-check receipt.
- Architectural effect: Gemma and Qwen now produce structurally isolated proposals, assess one another constructively, and feed configured Gemma Director synthesis. Deterministic projections, validation, routing, trace capture, and numeric budget enforcement surround the semantic loop; Plan remains passive.
- Tests/validation: Focused Normal-planning suite 9 passed; full suite 56 passed; `git diff --check` passed. CURRENT Archify showcase validation passed 9/9 with zero errors/warnings and delivery succeeded. Live smoke was blocked before calls because both configured endpoints refused connections. Automated browser visual-check was skipped because Chrome/Chromium is unavailable.
- Unresolved issue: Live five-call behavior remains unverified until both configured endpoints are running. DirectorTask generation, managed execution, TaskExecution construction, persistence, and ACCEPT/REVISE remain intentionally deferred to Iteration 3 or later.
- Archify map changed: Yes — `workspace/benzaiten_current.architecture.json` and `.html` now show the implemented reciprocal five-call planning path, semantic/deterministic authority split, passive final Plan, and unentered Iteration 3 execution seam. The frozen V0 TARGET map did not change.

#### Live validation follow-up — 2026-09-11

- Both configured model endpoints were started from their existing matching server profiles and passed `/v1/models` plus minimal inference checks. The first live Normal-planning run made both independent proposal calls, then correctly stopped before semantic advancement: Gemma returned JSON inside a Markdown fence, while Qwen returned visible `<think>` content and added fields outside the proposal contract. No assessment or synthesis calls were made, no retry occurred, and no task was executed.
- Added `scripts/open_dev_tabs.sh`, a development-only helper that waits for already-running Harness and Gemma services before opening their UI/docs with `xdg-open`; it does not start services or participate in Benzaiten runtime architecture.


#### Live-conformance follow-up — 2026-09-11

- Added deterministic whole-response normalization for plain JSON objects, exactly one outer `json` Markdown fence, or exactly one leading `<think>...</think>` block followed directly by a JSON object. Malformed JSON, arbitrary prose, combined/repeated envelopes, and unknown semantic fields remain validation failures. Proposal prompts now distinguish planning proposals from requested final results and require exactly the proposal fields.
- Focused tests passed 32/32 and the full suite passed 79/79; `git diff --check` passed. On the unchanged live smoke request, Gemma's outer fence was accepted, but Qwen reached its 800-token limit inside an unclosed `<think>` block and returned no JSON. Planning stopped after two reasoning calls with zero semantic advancements; no assessments, synthesis, final Plan, or execution occurred.
- Archify map changed: No — this is deterministic transport-envelope normalization and prompt conformance within the existing frozen Iteration 2 architecture.


#### Structured-output plumbing follow-up — 2026-09-11

- Verified that proposal `reason` and `proposal_reason` are distinct intentional Plan fields: `reason` explains the proposed approach, while nullable `proposal_reason` accompanies proposal-state disposition metadata (`decided_by_ref` and revision reference). Initial candidate schemas require `proposal_reason` and decision metadata to be null.
- Added generic `response_format` and `chat_template_kwargs` transport options plus exact-field JSON Schemas for all five planning stages. Capability metadata applies those schemas to Qwen; DiffusionGemma remains prompt-constrained with strict post-response validation because its vLLM endpoint explicitly rejects structured outputs for diffusion language models. Qwen now runs with the installed `qwen3` reasoning parser and disables thinking only for schema-constrained planning output.
- Recreated only the remote `benzaiten-qwen` container so its changed launch arguments took effect. Live Qwen proposal output became exact, complete JSON without reasoning leakage or extra fields. The final unchanged plumbing rerun still stopped after two calls and zero semantic advancements because Gemma emitted malformed JSON with a duplicate comma; deterministic code did not repair it. No assessments, synthesis, final Plan, or execution occurred.
- Archify map changed: No — this strengthens deterministic output plumbing within the frozen Iteration 2 architecture.

#### Semantic-ownership and assessment-semantics follow-up — 2026-09-11

- Planning models now author only stage-specific semantic content; deterministic Python supplies trusted proposal, assessment, and final-Plan protocol structure and runs the full validators after assembly. Both proposal branches assemble to the same canonical protocol shape, while Qwen uses semantic-only JSON Schema output and DiffusionGemma remains strictly prompt-constrained.
- Constructive assessment categories are exact-field arrays of zero or more non-empty findings. Empty arrays explicitly represent that no meaningful weakness, question, missing information, conflict, or other category finding was identified; the validator still rejects malformed values, unknown fields, and assessments with neither any finding nor any suggested change.
- Focused Normal-planning tests passed 46/46; the full suite passed 96/96; `git diff --check` passed. On the unchanged live fruit-sorting smoke, all five configured calls returned parseable semantic JSON with no retry: both proposals and both reciprocal assessments validated, consuming five reasoning tasks and two semantic advancements. Final synthesis was correctly rejected before Plan certification because Gemma Director accepted nonexistent suggested-change number 1 when both assessments proposed zero changes. No task was executed.
- Archify map changed: No — semantic/protocol ownership and objective empty-finding representation remain within the frozen Iteration 2 architecture.

#### Synthesis projection and conformance-repair follow-up — 2026-09-11

- Removed the static `[1]` synthesis examples, projected the exact supplied proposal, critique, and suggested-change references, and explicitly required empty change-number classifications when no suggested changes exist while retaining exact-once classification and strict invalid-reference validation.
- Wired the planning policy's generic `conformance_repair` path for structural conformance, invalid references, and protocol integrity. The original semantic producer receives its invalid output, exact deterministic issues, valid references, and semantic-preservation rules; at most one repair call consumes one reasoning task without semantic advancement, and Python revalidates without fabricating corrections.
- Focused Normal-planning tests passed 54/54; the full suite passed 104/104; `git diff --check` passed. The unchanged live fruit-sorting smoke certified a final one-step Plan on the initial synthesis, so no repair was required. It used five reasoning tasks and three semantic advancements. Calls totaled 3,753 prompt tokens, 789 completion tokens, and 4,542 tokens; recorded model latency totaled 8,767.13 ms (Gemma proposal 510.23 ms, Qwen proposal 2,117.42 ms, Qwen assessment 4,437.59 ms, Gemma assessment 380.15 ms, Gemma Director synthesis 1,321.74 ms).
- Archify map changed: No — this implements the target's bounded conformance-repair policy within the existing Iteration-2 semantic/deterministic authority split. The frozen V0 TARGET map did not change, and Iteration 3 remains unentered.

#### Focused Director synthesis-unit follow-up — 2026-09-11

- Replaced the monolithic Director synthesis artifact with focused semantic units: suggested-change disposition when changes exist, overall synthesis, and semantic Plan steps. Python maps complete dispositions to canonical change references, combines validated semantics, assigns trusted Plan/revision/step IDs and bookkeeping, and certifies the canonical Plan. Zero suggested changes skip the disposition call. All focused Director calls remain on the configured `gemma_director` context and at semantic iteration 2; only complete Plan certification advances to iteration 3.
- Added planning-policy `semantic_completion` for omitted required semantic decisions. The same semantic producer receives the incomplete artifact, exact missing decisions, valid references, and a missing-decisions-only scope; one attempt consumes a reasoning task without semantic advancement. Structural conformance, invalid references, and protocol integrity continue through the distinct one-attempt conformance-repair path. No RepairManager, CompletionManager, Router, or other manager was added.
- Focused Normal-planning tests passed 59/59; the full suite passed 109/109; Python source compilation and `git diff --check` passed. Tests cover exact-once dispositions, the zero-change path, strict nonexistent references, completion versus repair routing, same-producer context, reasoning and semantic counters, trusted Python bookkeeping, non-invention, final certification, and honest termination after invalid second outputs.
- The requested live static model-routing planning problem structurally certified in seven reasoning tasks and three semantic advancements, with three suggested changes all accepted and no semantic-completion or conformance-repair call. Total recorded model latency was 31,938.17 ms; calls used 10,195 prompt tokens, 2,429 completion tokens, and 12,624 total tokens.
- Live semantic issue: the certified Plan assigns deterministic model selection to the Director and says it returns an abstract model ID. That conflicts with the frozen authority rule that the Director selects semantic requirements while configuration and the Task Executive resolve the concrete model mechanically. The prompt projected the authority constraints, but current deterministic validation certifies protocol structure rather than this semantic claim. This must be resolved before Iteration 3; no execution work was started.
- Archify map changed: Yes — `workspace/benzaiten_current.architecture.json` and `.html` describe focused Director semantic units, Python-owned assembly, variable six/seven base reasoning calls, and completion/repair remaining at semantic iteration 2. The frozen V0 TARGET map did not change.

#### Semantic architecture acceptance follow-up — 2026-09-11

- Added a configured advisory semantic-architecture assessment after deterministic structural certification. The assessment checks the twelve frozen Director, Plan, Task Executive, routing, static-metadata, and fallback invariants without gaining decision authority. A noncompliant candidate may receive exactly one semantic boundary correction from the same configured Director, followed by canonical Python reassembly, structural recertification, and a second advisory assessment. Correction calls consume reasoning budget and remain at semantic iteration 2; only dual acceptance advances to iteration 3.
- Focused Normal-planning tests passed 68/68; the full suite passed 118/118; Python source/test compilation and `git diff --check` passed. Coverage includes compliant acceptance, concrete-model/Router/implicit-fallback violations, deterministic Task Executive/configuration language, configuration-routed assessment, exactly one correction, reasoning and semantic counters, structural certification before both assessments, successful correction, and honest second-failure termination.
- Both configured live endpoints were healthy. The requested static model-routing planning run produced a structurally certified Plan that the configured `gemma_worker` assessor accepted with zero violations, so no semantic boundary correction was required. The Director disposition artifact did require one same-producer conformance repair for malformed outer JSON; the repaired four-change classification accepted changes 1, 2, and 4 and deferred change 3. The run completed with nine reasoning tasks, three semantic advancements, one structural certification, 37,477.64 ms recorded model latency, 16,060 prompt tokens, 2,517 completion tokens, and 18,577 total tokens.
- The accepted Plan keeps the Director at semantic-requirement authority, assigns concrete resolution mechanically to configuration-driven routing logic, contains no hard-coded model branch or third intelligent manager, uses static capability metadata and deterministic priority tie-breaking only, and makes local fallback an explicit configuration entry. Historical proposal text that assigned resolution to the Director was not adopted into authoritative synthesis.
- Iteration 2 is complete. No DirectorTask was generated, no Plan step or TaskExecution was executed, and no ACCEPT/REVISE or Iteration-3 runtime work began.
- Archify map changed: Yes — `workspace/benzaiten_current.architecture.json` and `.html` now show structural certification plus configured semantic acceptance and the bounded Director correction loop. Showcase delivery passed 9/9 checks with zero errors/warnings; automated browser visual-check remained skipped because Chrome/Chromium is unavailable. The frozen V0 TARGET map did not change.

#### End-of-day architecture checkpoint — 2026-09-11

- Added `docs/architecture/2026-09-11_managed_work_v0_checkpoint.md` as the concise Iteration-1/Iteration-2 handoff, including the validated state, live endpoint evidence, observed Qwen/Gemma latency split, and the already-established Iteration-3 milestone sequence without designing or implementing it.
- Regenerated the CURRENT Archify map so Candidate Plan, deterministic structural/protocol certification, configured semantic architecture acceptance, and final certified passive Plan are distinct nodes. The one-attempt semantic-boundary correction is an explicit return to the existing Director, not another authority; future model resolution and execution remain assigned to Task Executive/configuration beyond the unentered Iteration-3 seam.
- Archify showcase delivery passed 9/9 checks with zero errors/warnings. Specification SHA-256: `bc6c5695c91dc41f8764d2359d21934aca2b50964c136fe40dead308fadca512`; HTML SHA-256: `81518e273b94fbeb6b0847d2c86d1f1e7367df9b6fb4a86ae22c6263ddb6116e`. Automated browser evidence was skipped because Chrome/Chromium is unavailable, so perceptual review remains pending.
- Documentation/architecture only: runtime behavior, tests, and the frozen Managed-Work V0 TARGET were not modified. Iteration 2 remains complete; Iteration 3 remains unentered.

### Iteration 3 — 2026-09-15

- Goal: Implement and validate the checkpoint’s first in-memory certified-Plan handoff through Director step selection, DirectorTask, deterministic validation, configured worker execution, TaskExecution, and Director evaluation; no planning redesign in this foundation slice.
- Files changed: `src/director.py`, `src/orchestrator.py`, `src/task_execution.py`, `protocols/execution/task_execution_protocol_v0.json`, `protocols/execution/task_execution_vocabulary.yaml`, `smoke_tests/test_managed_work_iteration3.py`, this ledger, and the CURRENT Archify JSON/HTML/visual-check receipt.
- Architectural effect: The configured Director selects only among mechanically eligible Plan steps and authors semantic-only task content. Python assigns trusted task identity and job/Plan/step/input/output-contract references, while Task Executive revalidates the active job handoff, resolves role → logical context → model from configuration, enforces the reasoning-call limit, invokes generic ModelClient transport, mechanically compares decidable output, and constructs the authoritative protocol-shaped TaskExecution. The Director receives a narrow evidence projection and authors the linked ACCEPT/REVISE outcome stored in existing `plan_execution` fields.
- Validated transitional behavior: A representation-invalid task selection or evaluation receives at most one conformance repair from the same configured Director. The implemented controller stops on ACCEPT, permits one new DirectorTask and execution for the same certified Plan step after a first REVISE, stops unresolved after a second REVISE, and reports `replan_not_implemented` when Plan change is requested. Those bounded same-Plan REVISE semantics were validated as part of the 147-test execution foundation but are superseded by the recovered 2026-09-14 graph/revision policy. They are not the final Iteration-3 lifecycle. Representation repair remains valid and distinct from semantic REVISE.
- Tests/validation: Coherent-part focused suites passed 21, 35, 42, and 48 tests respectively. Final focused Iteration-3 file passed 29 tests; the full repository suite passed 147 tests; Python source/test compilation and `git diff --check` passed. CURRENT Archify showcase validation and delivery passed 9/9 checks with zero errors/warnings. Specification SHA-256: `4a094694459d54bfb0213cbd502aa6cab796bce47c2515f4e14c8e60fd9938d6`; HTML SHA-256: `12948fd131b7504f11293d0ae348b77ecd654f56c827b31c8c28da81d1b70851`. Automated browser evidence was skipped because Chrome/Chromium is unavailable, so perceptual review remains pending.
- Unresolved issue: This slice is deterministic/mock validated but not yet run live against both configured endpoints. Reconciliation must add a separate lineage-derived execution-transition budget, `ACCEPT | REVISE | ASK_GUIDANCE`, immutable successor/backtracking Plan revisions, accepted checkpoint identity, persistence/resume, persisted user-controlled frontier policy, and configured resource execution. Filesystem artifact persistence, current communication-envelope persistence, and a full planning-to-execution bootstrap remain outside this in-memory foundation; TaskExecution records therefore truthfully mark persistence as disabled.
- Archify map changed: Yes — `workspace/benzaiten_current.architecture.json` and `.html` now show the implemented DirectorTask, Task Executive, TaskExecution, ACCEPT/REVISE, same-step revision, and authority boundaries. The frozen V0 TARGET map did not change.

#### Recovered Iteration-3 design authority — 2026-09-15

- Preserved the complete substantive 2026-09-14 design as `docs/architecture/2026-09-14_managed_work_iteration3_design_record.md` and added the frozen restart decisions to the managed-work checkpoint.
- The 147-passing implementation remains the execution foundation. Only its controller lifecycle is superseded: semantic REVISE now means immutable successor-Plan revision/backtracking, not a bounded retry against the same Plan.
- This documentation amendment changes no runtime behavior. Subsequent reconciliation is intentionally incremental and must retain the existing regression baseline.
- Root-checkpoint clarification: the planning-certified `@r1` is the distinguished root and uses its trusted planning finalization/certification evidence without fabricating an execution ACCEPT. Every later execution checkpoint requires a real ACCEPT TaskExecution outcome.

#### Graph lifecycle, resume, and resource reconciliation — 2026-09-15

- Preserved the complete recovered design in `docs/architecture/2026-09-14_managed_work_iteration3_design_record.md` and made the checkpoint’s `Iteration 3 — frozen decisions` section the concise restart authority. The distinguished `@r1` exception is explicit: root checkpoint evidence comes from trusted planning finalization and certification, while every later execution checkpoint requires a real `ACCEPT` TaskExecution outcome.
- Expanded the existing TaskExecution outcome to strict `ACCEPT | REVISE | ASK_GUIDANCE` records with trusted identity, Director reason/evidence, Plan and outcome checkpoint references, successor reference, and complete optional guidance semantics. Python validates and records; the Director authors meaning.
- Added the separate configured `execution_transitions` budget. V0 derives consumption from immutable chronological Plan history (`revision - 1`), keeps planning fixed at 3/3, and creates no mutable counter or `JobContext`.
- Replaced the transitional same-Plan REVISE controller with immutable successor Plan assembly. Revisions retain `plan_id`, increase chronologically, use `based_on_revision_ref` as the graph edge to a certified root or execution-accepted checkpoint, qualify new step IDs by revision, preserve failed branches, and link triggering execution/outcome evidence.
- Added passive managed-work persistence under the configured workspace artifact root: immutable request, Plan, DirectorTask, TaskExecution, and call records plus one atomically replaced `resume.json`. Reload revalidates lineage/checkpoints, reconstructs the awaiting-guidance seam, and recovers reasoning consumption from reasoning-bearing call artifacts.
- Persisted the user-owned guidance policy (`USER_ONLY`, `ASK_BEFORE_FRONTIER`, or `FRONTIER_ALLOWED`) and explicit frontier authorization. Actual frontier targeting requires both authorization and a configured provider; V0 config truthfully has no frontier provider.
- Replaced the stale mandatory question-resolution order with unordered Director-selected resources. DirectorTask selection now uses numbered semantic inputs and a semantic `text | json_object` output kind; Python maps both to trusted references. Task Executive reads sandboxed configured Vault/OKF and source files, invokes a configured web provider only when present, otherwise records explicit tool failure with no fallback, and continues to invoke configured local model workers through generic ModelClient transport. No research coordinator or intelligent router was added.
- Corrected the older expected-value comparison policy. Python now validates only the configured output representation; structurally valid semantic disagreement reaches the Director. Malformed worker representation receives at most one repair from the same worker and does not become semantic REVISE.
- Tests/validation: incremental focused suites passed after each outcome/budget, lineage, lifecycle, persistence, authorization, resource, and output-contract part. The final focused reconciliation suite passed 76/76; the full repository suite passed 173/173; Python source/test compilation and `git diff --check` passed.
- Remaining implementation seams: no live endpoint run was performed in this reconciliation; no frontier provider is configured; configured agent-process mechanics and incorporation of returned guidance into a successor Plan remain later incremental work. The persisted ASK_GUIDANCE state is reloadable and non-terminal, but this entry does not claim those later integrations complete.
- Archify map changed: Yes — CURRENT now shows the implemented DirectorTask → Task Executive → TaskExecution loop and its graph/checkpoint/persistence/resource authority. Showcase validation/delivery passed 9/9 checks with zero errors/warnings (specification SHA-256 `c57bad4c94f0802bbde73637197d0e8fa67803374e21885db2f089be03de1e36`; HTML SHA-256 `0d0de8e986b86a95c468ae58bc1c4d93ca2b50ea74f77bfd218d68e914917638`). Automated browser containment/readability passed at 1440×900, 1600×1000, 1920×1080, and 2048×1320; perceptual review remains pending. The frozen V0 TARGET source, HTML, and tracked receipt remain unchanged.

#### Live vertical execution proof — 2026-09-15

- Added `src/run_managed_work.py` as a thin synchronous launcher. It loads and validates one persisted planning-certified `@r1` plus resolved inputs, carries prior reasoning-call consumption across the planning/execution seam, refuses implicit replay when a validated resume state already exists, and delegates unchanged to `run_iteration_3`. It performs no step selection, worker resolution, or semantic judgment. The controller now returns its already-validated Director selection semantics for live-proof inspection alongside the trusted DirectorTask.
- No canonical managed-work Plan artifact existed in the repository. The existing live Normal-planning pipeline therefore produced and certified `plan-live-managed-work-20260915@r1` from the configured Gemma/Qwen contexts in 10 reasoning calls and three planning iterations. The Plan contains one passive executable step and passed deterministic certification plus configured semantic architecture assessment before execution.
- Live job `job-live-managed-work-20260915-004` completed the configured boundary: `gemma_director` selected semantic work; Python assembled the trusted DirectorTask; Task Executive mechanically resolved `primary_worker` to `gemma_worker` and `diffusion_gemma` on `pc1.gpu0`; generic ModelClient transport invoked the configured endpoint; TaskExecution recorded `{"message":"Benzaiten live managed-work proof","verified":true}`; and `gemma_director` returned `ACCEPT` with `continue_work=false`. No fallback, frontier call, resource route, or semantic selection outside the Director occurred.
- The live job continued reasoning accounting at 10 and recorded calls 11–14: Director selection, one same-Director selection repair, configured worker execution, and Director evaluation. The repair corrected malformed representation (`selected_input_numbers` string to integer and an extra brace) without changing semantic work. Worker output and final evaluation required no repair.
- Persistence wrote the immutable request, `rev-0001` Plan, DirectorTask, TaskExecution, and four call request/response pairs plus the atomically replaced resume pointer. Production reload validation recovered reasoning count 14, the unchanged `@r1` current Plan, zero execution transitions consumed, and completed resume state. The accepted checkpoint resolves to the real `ACCEPT` outcome `execution-f561c3aee45a46538e43bbb81585f576:outcome-b489301a7d7a44779536be4b1957b77c`, which references `plan-live-managed-work-20260915@r1` and execution `execution-f561c3aee45a46538e43bbb81585f576`.
- Live evidence exposed and corrected three generic conformance/wiring defects without changing authority: the bootstrap initially omitted prior reasoning consumption; the shared conformance classifier omitted existing selection codes `duplicate_reference`, `missing_plan_input`, and `unconfigured_role`; and non-schema Director prompts did not make contextual vocabulary or evaluation nullability constraints explicit. Validation remained strict, each producer retained at most one representation repair, and no semantic disagreement was reclassified as conformance.
- Before the successful job, explicitly named attempts stopped honestly at their failing boundary: one classifier dispatch failure, one evaluation whose single repair remained inconsistent, and two selection repairs that remained malformed. They produced no TaskExecution outcome or persisted managed-work state and were not hidden retries inside the controller.
- Tests/validation: pre-change focused suite 69/69; final focused managed-work/planning/bootstrap suite 123/123; full repository suite 178/178; Python source/test compilation and `git diff --check` passed. Production persisted-state reload also passed.
- Remaining unvalidated live paths: semantic `REVISE` with successor/backtracking execution, persisted/resumed `ASK_GUIDANCE`, Vault/source/web resource execution, Qwen as the Director-selected execution worker, configured agent-process mechanics, frontier advice/provider behavior, and returned-guidance incorporation. These retain their prior deterministic coverage or documented implementation status and are not claimed live here.
- Archify map changed: No — the represented DirectorTask → Task Executive → TaskExecution architecture did not change; this work added its bootstrap and live validation evidence plus prompt/conformance hardening. The frozen TARGET remains unchanged.

#### Live REVISE proof attempts — planning-fidelity finding — 2026-09-15

- The A–C/A–L attempt is not evidence of an Iteration-3 REVISE failure. Its persisted frozen request required exactly one Plan step and required the deliberate mismatch between A–C-only execution and a complete A–L expected result to remain unresolved. Live Normal planning instead certified two coherent steps: an A–C-only first step whose expected result became an A–C provisional verdict, followed by a full A–L audit step. The live Director correctly returned `ACCEPT` because the completed TaskExecution satisfied the certified first-step contract. No successor was created and zero execution transitions were consumed.
- A replacement proof used a coherent evidence-timing boundary: planning knew only that an authoritative constraint source would be supplied at execution, while the detailed values were withheld until after certification. The frozen request reasonably expected one feasible configuration and explicitly distinguished a correct impossibility report as useful execution evidence rather than fulfillment of that expected result.
- Three live Normal-planning candidates remained structurally and architecturally certifiable but were unsuitable for REVISE validation. The first decomposed the operation into ingestion, derivation, and validation, making source ingestion the initially eligible result. The two atomic candidates changed the expected result to accept either a feasible configuration or an incompatibility report. That semantic broadening made the intended anti-solution an `ACCEPT`-quality result before execution evidence existed.
- These observations are planning/certification semantic-fidelity findings, not changes to or failures of the Iteration-3 execution graph. Normal planning and certification were not weakened or patched. The unsuitable candidates were not executed, no Director outcome was prescribed or fabricated, and no runtime or protocol behavior changed.
- Live semantic `REVISE`, immutable successor creation, and graph backtracking therefore remain unvalidated against real endpoints. They retain deterministic test coverage only. The frozen TARGET remains unchanged, and CURRENT requires no update because represented architecture did not change.

#### Request-to-Plan semantic fidelity correction — 2026-09-15

- Goal: Preserve the original user mandate through Normal planning, semantic Plan acceptance, execution judgment, and successor acceptance without redesigning the validated Iteration-3 path. The complete accepted diagnosis was saved before runtime edits in `docs/architecture/review/2026-09-15_request_to_plan_semantic_fidelity.md`.
- Files changed for this correction: `protocols/planning/planning_policy.yaml`, `src/planning.py`, `src/director.py`, `src/orchestrator.py`, `src/run_managed_work.py`, `smoke_tests/test_normal_planning.py`, `smoke_tests/test_managed_work_iteration3.py`, `smoke_tests/test_run_managed_work.py`, the checkpoint, this ledger, and the dated review. Existing local bootstrap/live-proof work was preserved.
- Policy/projection: One shared model-facing rule distinguishes response conformance, truthful evidence, selected-work fulfillment, and overall-mandate fulfillment. It preserves truthful failure reporting and legitimate negative diagnostic answers, while forbidding unauthorized objective/constraint/success broadening. The rule and complete original request reach proposals, reciprocal assessments, disposition/synthesis, root acceptance/correction, execution selection/evaluation, and successor synthesis/acceptance/correction. Deferred evidence is not automatically converted into broader success or guidance.
- Original-request handling: The controller resolves a frozen copy of the root's exact request input from the existing mapping; missing contents fail explicitly rather than falling back to Plan wording. Generated `request:<sha256>` references are checked before execution, persistence, and reload. Existing trusted opaque references remain supported. Immutable `request.json` remains the storage route; no mandate protocol field or semantic Python comparison was added.
- Shared gate: Extracted the existing bounded resolver and semantic acceptance/correction mechanism for reuse by root and successor paths. The configured advisory assessor now checks architecture and original-mandate fidelity. One same-Director semantic correction and reassessment remain allowed; architecture-named compatibility keys and record shapes are retained. Completion of omitted gate judgments receives the original request, candidate, architecture invariants, and applicable execution context. Representation repair remains distinct from missing-judgment completion and semantic correction.
- Successor publication: Existing structural/lineage assembly creates a candidate; the shared semantic gate must accept it before the controller links `resulting_plan_ref` or appends the immutable revision. The gate receives the real triggering execution/outcome and checkpoint context, and charges existing reasoning budgets. Rejection after bounded correction, exhausted gate recovery, or an exhausted gate reasoning budget returns explicit `semantic_plan_unresolved`, preserves the real execution/outcome and prior lineage, and persists existing `unresolved` resume state without consuming a transition. Gate prompts/responses are retained in the existing call artifacts; the launcher now exposes assessment, correction, and error diagnostics.
- Completion semantics: The Director evaluation prompt requires legitimate selected-work fulfillment for ACCEPT and overall original-mandate fulfillment for `continue_work=false`/completed. It receives all current Plan steps and prior execution/outcome evidence. Python still validates only representation and obeys the Director's semantic judgment; no wording-based success classifier or silent `expected_result` rewrite was introduced.
- Tests/validation: Managed-work/bootstrap focused suite 68/68; with Normal planning 140/140; full repository suite 195/195 (reported starting baseline 178). Python source/test compilation and `git diff --check` passed. Added 17 deterministic cases covering deliverable/diagnostic policy projection, unauthorized broadening and bounded root correction, missing/tampered original requests, completed impossibility evidence with persisted ASK_GUIDANCE, intermediate ACCEPT versus overall completion, authorized successor/correction and failed-publication behavior, grounded bounded assessor repair/completion, and gate-budget exhaustion. Existing lineage, checkpoint, frontier-authorization, routing, and conformance regressions remain green.
- Evidence limits and remaining seams: These tests supply model decisions and prove wiring/contracts, not model reasoning. No live models or fidelity/ASK_GUIDANCE/REVISE scenarios were run. The earlier live ACCEPT proof remains historical evidence for the execution boundary, not validation of this new fidelity behavior. Pre-certification failure remains explicit and non-resumable: no root/checkpoint/execution/guidance state is fabricated. Incorporating returned execution guidance remains the previously deferred seam. No retroactive semantic reassessment of existing certified artifacts was performed.
- Archify map changed: No — the existing assessor, Director, Task Executive, passive Plan, and persistence mechanisms were reused; their expanded semantic acceptance scope is recorded here and in the checkpoint. ModelClient and the frozen TARGET remain unchanged.
